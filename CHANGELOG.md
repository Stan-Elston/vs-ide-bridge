# Changelog

## 3.0.4

Consolidated release rolling up all bridge and installer fixes since 3.0.3.

### Installer

- The Visual Studio extension step is now reliable and no longer force-closes an in-use VS. Root cause of the recurring "the extension won't update" failures: `VSIXInstaller /install` is blocked by *any* VS process it recognizes, and repeated builds leave dozens of zombie `MSBuild.exe` (plus `VBCSCompiler.exe` / `ServiceHub.*`) alive, so every install failed with exit 2004 and left the extension stuck at the old version (with an unprocessed `extensions.configurationchanged` marker). The installer now clears those background build processes automatically before the VSIX step (they hold no user data). A leftover *windowless* `devenv.exe` zombie is also cleared automatically with no prompt (detected via `tasklist /V`, whose window title is `N/A` for a zombie vs. `Microsoft Visual Studio` for a real instance); only a genuinely in-use Visual Studio window triggers a warning (a plain `MsgBox`: Yes = close & continue, default / No = retry / Cancel = skip the extension). The check runs before the post-install progress page so the dialog keeps modal focus.
- The post-install VSIX step is a single `VSIXInstaller /install`. The previous `uninstall-current` + `uninstall-legacy` launches were redundant (the prior version's uninstaller, run first from `InitializeSetup`, already removes the old extension) and were the bulk of the ~45s perceived install "hang" (three VSIXInstaller cold-starts where one suffices). The service stop is not the bottleneck (it finishes in ~40ms).
- Fixed VSIXInstaller `/logFile` pointing at an invalid path (`{log}\name.log`, where `{log}` is the Setup log *file*, not a directory), so VSIX install/uninstall logs were never written. They now go to `{commonappdata}\VsIdeBridge\logs`.

### Diagnostics & analyzers

- Failed builds whose only errors appear in the Build output pane (linker `LNK2001`/`LNK1120`, `MSB####`) now surface as structured `buildOutputErrors` via a shared `BuildOutputErrorParser`, with `nextStep` guidance pointing at `read_output {pane:"Build"}` when the Error List disagrees. The parser also keeps warning rows and attaches project-code origin metadata (`originFile`/`originLine`/`originColumn`) for MSVC-header/template diagnostics, resolving relative CMake paths against the nearest solution ancestor.
- Diagnostics summaries no longer say "fix all before building" when only warnings/messages remain, and always show a full severity breakdown so a model cannot declare "0 errors" clean while warnings/messages remain. Content-filtered reads lead with the filtered population and label whole-Error-List totals separately.
- Grouped diagnostics (`warnings {group_by:"code"}` etc.) report `countScope`/`groupCount`/`diagnosticCount` so group rows are not misread as diagnostic counts, and grouping now spans the full filtered set even with a finite `chunk_size` (the client's chunking paginates the groups; the VSIX is asked for the full row set). Grouped `group_by` pagination also actually paginates the groups instead of returning all of them on every chunk.
- Diagnostics cache freshness fixes: live full-bucket reads refresh the matching service cache bucket, filtered reads invalidate that severity bucket instead of leaving stale rows, and handle-based `file`/`path` filters bypass the passive cache so handles and filenames use the same freshness path.
- Analyzer false-positive guards: BP1022 (raw `new`), BP1025 (by-value params), BP1008 (C-style cast), and BP1014 (single-letter variable) now skip matches inside comments and string literals (and BP1008 handles unnamed `const` params and gives C-appropriate guidance in `.c` files). VCR001/VCR003 rows are labeled analyzer-limited (VS's own linter can lag/misread modern C++ like `= delete` and explicit specializations).

### apply_diff / documents

- Multi-edit `apply_diff` stops at the first failed edit (later edits often depend on earlier ones), reports the rest as `skipped`, and labels the result `PARTIAL MUTATION`.
- `apply_diff`/`write_file` no longer silently skip the requested save when the editor window loses its `Document` reference after a large buffer replace; the save re-resolves the open document by path and the `saved` field reports the real state. Timeout envelopes for document-mutating commands now say a timeout does not undo the edit and to verify/save.
- `save_document` resolves handles (`f:29`) and repo-relative paths via the same `DocumentService` resolver as reads/edits, and an omitted `file` now correctly saves all open documents (`all=true`) as documented.
- `apply_diff` no longer patches a guessed location when every context line is trivial (blank lines / lone braces); it fails with guidance to include a distinctive line.
- `compile_file` and positioned `execute_command` resolve source-relative paths in out-of-tree builds via the full document resolver instead of normalizing into `build-default\src\...`.

### Connection & reliability

- Fixed the stale-binding failure mode where every bridge call after a timeout kept failing until a manual rebind: connect attempts are capped (~10s), a dead pipe is evicted and retried with fresh discovery, the per-pipe lock file is disposed on failure, and a response-timeout reports `SessionLost` only when the VS process is actually gone.
- Timed-out commands no longer execute concurrently with the next request (the abandoned command owns the queue release; a blocked request gets a `bridge_busy` envelope).
- Fixed the request-execution fallback producing invalid JSON (newlines in Win32 exception messages broke the pipe protocol); it now serializes a real envelope.
- Fixed connection tasks leaking on VS shutdown (net472 `ReadLineAsync` has no cancellation overload).
- `search_symbols` `kind` filter works against text-source matches (C/C++ solutions) via `inferredKind`; `find_text_batch` accepts the documented `{query, path}` object form; `find_text`/`search_symbols` relative directory path filters stay substring filters instead of becoming exclusive rooted prefixes under the build directory.
- The bridge reports its own version everywhere it identifies itself (discovery JSON, `vs_state`, `bridge_health`, bind responses).

### Watchdog

- Watchdog probe timeouts while a foreground bridge command holds the UI thread are classified as busy (`isBusy`, `totalBusyProbes`) instead of degraded; a probe timeout with no command running still degrades.

### HTTP / service (LocalSystem)

- Fixed the LocalSystem service (session 0) misclassifying every interactive VS as dead and deleting its discovery file: `IsLiveInstance` required a visible `MainWindowHandle`, but window handles are session-scoped, so over HTTP `visibleInstanceCount` went to 0 and the VSIX's pipe JSON was deleted. The zombie-window heuristic now only applies within the same session.
- Fixed `memory_search`/`memory_read` failing under LocalSystem with the memory root resolving under `C:\WINDOWS\system32\config\systemprofile\.codex`. The interactive user's home is now derived from a discovery-file path (the bound instance's, or any discovered instance's when a fresh HTTP session is unbound). `memory_search` also gained ranked multi-term queries.

### Encoding

- Replaced every corrupted separator character in bridge source strings (17 lines across 10 files carried a literal U+FFFD or invalid Windows-1252 dash byte, visible as mojibake in `bridge_health`/tool descriptions/advisories, plus two double-encoded em dashes in `PatchService`). Every source file decodes as valid UTF-8.

### Platform & packaging

- The VSIX now targets Visual Studio 2022 in addition to 2026 (`[17.14,19.0)`; 17.14 is the SDK floor).
- Made the `python_eval`/`python_exec` scratchpad usable beyond arithmetic: the `__builtins__` allowlist is removed (full builtins + `import`), `re`/`itertools`/`collections` are pre-imported, snippets run in a single namespace so top-level helpers see each other, and non-JSON values serialize via `default=str`.
- Fixed `scripts/build.bat` failing with "vswhere.exe not found" in environments without `ProgramFiles(x86)` by adding a literal-path fallback.

## 3.0.3

- Added GitHub issue write tools so models can manage issues without falling back to shell `gh`: `github_issue_comment`, `github_issue_close` (optional `reason` and `comment`), `github_issue_reopen`, `github_issue_edit` (comma-separated `add_labels`/`remove_labels` plus `title`/`body`), and `github_issue_create` (`title`, optional `body`, comma-separated `labels`). Each runs through `GhRunner` in the resolved repository directory against gh's default repo, mirroring the existing read-only `github_issue_*` tools. `github_issue_close` is classified destructive and the rest mutating; the issue registrations were split into `GitHubIssueCommentAndCloseTools` / `GitHubIssueEditAndCreateTools` to keep methods small, and the tool-surface tests now assert the new tools' flags.
- Added bridge tool coverage for richer repository triage: `git_file_history`, `git_blame`, `git_merge_base`, `git_cherry`, and `git_conflicts`; GitHub PR discussion/status readers (`github_pr_comments`, `github_pr_reviews`, `github_pr_review_threads`, `github_pr_checks`, and `github_actions_failed_logs`); read-only Codex memory tools (`memory_search`, `memory_read`); and `bridge_installed_version` for confirming the deployed bridge binaries. Updated default recommendations, the memory category, Git-category metadata, and tool-surface tests so models can discover and use the new tools.
- Made project files discoverable and directly targetable for bridge edits. `query_project_items` now returns the owning project file itself as an `itemType: "ProjectFile"` row with `isProjectFile: true`, so callers can feed `.csproj`, `.vcxproj`, `.props`, `.targets`, and related project files into `read_file` / `apply_diff` without falling back to disk-only searches. `PathResolver` also checks loaded `Project.FullName` values when resolving bare filenames, so `apply_diff` can intentionally resolve a loaded project file through the active solution model before filesystem fallback. Updated project-tool and `apply_diff` help to distinguish project membership tools (`add_file_to_project` / `remove_file_from_project`) from editing the project XML itself.
- Modernized debugger exception capture so `lastException` is actually populated. The previous implementation depended on the legacy DTE `DebuggerEvents.OnExceptionThrown`/`OnExceptionNotHandled` event sinks, which only fire for the exception categories the user configured to break on (managed code defaults to user-unhandled only) and carry only a thin type/code/description -- so in practice `lastException` was usually empty and a model that hit the non-modal Visual Studio Exception Helper saw an unexplained break it could not read. `DebuggerExceptionTracker` now actively evaluates the `$exception` pseudovariable (the same object the Exception Helper shows) whenever the debugger is in break mode, surfacing the real runtime `type`, `message`, `stackTrace`, `hResult`, and `innerException`, alongside `event` (`thrown`/`notHandled`/`inFlight`) and `breakReason`. The pull-based read runs on the UI thread inside `GetStateAsync` and `GetExceptionsAsync` (so it reaches every `debug_start`/`debug_continue`/`debug_step_*`/`debug_break`/`debug_exceptions` response), no longer depends on the flaky events, and only `OnEnterRunMode` is kept (to clear a stale capture when execution resumes). The Exception Helper itself is non-modal, so a caller dismisses it by reading the now-populated `lastException` and calling `debug_continue` or `debug_stop`.
- Added a native debugger-event fallback for `lastException`. `DebuggerExceptionTracker` now subscribes to `IVsDebugger.AdviseDebugEventCallback` and captures `IDebugExceptionEvent2` payloads when native C++ debugging breaks on an exception that does not produce a useful DTE `$exception` object, surfacing the native exception name, description, code, state flags, GUID type, program name, and capture timestamp through debugger state and `debug_exceptions`.
- Quieted a noisy `DocumentService` warning. The editor-selection navigation fallback (`TryNavigateToLineFallback`) logged `TextDocument.get_Selection` COM `E_FAIL` at WARNING on every miss, even though failing to position the caret is an expected best-effort outcome (document not fully initialized, or the active view is not a text editor). Downgraded to verbose so it no longer spams the shared log file.
- Hardened and instrumented the breakpoint condition-dialog auto-dismiss. The WH_CBT hook now also matches a window whose title is exactly "Microsoft Visual Studio" (the dialog's own title) in addition to the classic `#32770` dialog class, so a themed/WPF dialog is covered too (the previous class-only match could miss it). Added `BridgeActivityLog.LogInfo` (INFO to the shared log file) and the hook now logs install plus the class/title/body-text/dismissal of every candidate dialog, so the failure mode is diagnosable directly from the log.
- Made breakpoint remove/enable/disable consistent with `set_breakpoint`: `remove_breakpoint`, `enable_breakpoint`, and `disable_breakpoint` now accept a `function` argument, so a breakpoint set by symbol (`set_breakpoint { function: ... }`) can also be removed or toggled by symbol instead of only by file:line. `file`/`line` are now optional on those tools (supply either `file`+`line` or `function`).
- Made file/dir arguments accept bridge handles consistently across tools. `add_file_to_project`, `remove_file_from_project`, and the Python interpreter / startup-file commands now route their path through `HandleService.ResolveFilePath` (a no-op for plain paths) like `read_file`/`apply_diff`/`set_breakpoint` already do, so an `h:`/`f:` handle works anywhere a file path does -- models no longer have to track which tools take handles and which take raw paths.
- Added container/struct expansion to `debug_locals` and `debug_watch`. Both tools now accept `expand_depth` (0 = summary only, the previous behaviour; 1–3 recursively walks the value's child members like expanding the node in the Visual Studio Watch window) and `max_children` (per-level cap, default 50). `SerializeExpression` recurses through `EnvDTE.Expression.DataMembers`, emitting a `members[]` array per node plus `membersTruncated`/`membersOmitted` when a level exceeds the cap and `membersError` if the COM enumeration throws. This lets a caller inspect the contents of a `std::vector`/`list`/`map` or struct (e.g. every element of a `std::list` and its fields) in a single call instead of only the debugger's summary string.
- Added function/symbol breakpoints to `set_breakpoint`. Passing `function` (e.g. `"VsIdeBridgeService.ToolResultFormatter::StructuredToolResult"`) binds the breakpoint by symbol via `EnvDTE.Debugger.Breakpoints.Add(Function:)` instead of file/line, so it survives source edits and line shifts that invalidate file:line breakpoints. `file`/`line` are now optional in the schema (either `file`+`line` or `function` is required, validated in `BreakpointService`); function breakpoints skip the editor reveal step. A duplicate breakpoint on the same function is replaced in place, and an unbound result is reported as `pending` (e.g. the owning module is not loaded yet) rather than failing. Matching uses `Breakpoint.FunctionName` (exact or suffix match).
- Exposed tracepoint/logpoint parameters in the `set_breakpoint` tool schema. The VSIX command and service already applied `trace_message` + `continue_execution` via `Breakpoint2.Message`/`BreakWhenHit`, but the MCP tool catalog never surfaced the parameters, so they were unreachable. `set_breakpoint` now advertises `trace_message` (logged when the breakpoint is hit; supports `{expression}` interpolation), `continue_execution` (log and keep running instead of pausing — a tracepoint/logpoint), plus the already-wired `condition_type` (`when-true`/`changed`) and `hit_type` (`none`/`equal`/`multiple`/`greater-or-equal`). This replaces ad-hoc trace-to-file instrumentation and manual continue loops for high-frequency value tracing.
- Added in-memory paging to `debug_watch` for large expansions: `chunk_lines` (lines per chunk) and `chunk_index` page the expanded member tree through the bridge the same way `read_output` pages a pane. With `chunk_lines > 0` the response returns `membersJson` (the selected chunk) plus `chunkIndex`/`chunkCount`/`totalLines`/`hasMoreChunks`; advance `chunk_index` to read the rest. The full result stays in the bridge's memory and is re-derived per call (no server-side cursor), so a big container/struct can be pulled piecewise without flooding the reply. `debug_watch` also lists the Core `batch` tool in its search hints, since several watch expressions can be evaluated in one round-trip by batching `debug_watch` calls (no dedicated multi-expression tool was added).
- Added auto-capture and dismissal of the modal breakpoint condition-failure dialog. When a breakpoint condition or tracepoint expression cannot be evaluated during debugging (e.g. it calls a function the expression evaluator treats as having side effects, such as `std::list::size()`), Visual Studio shows a modal "The condition for a breakpoint failed to execute" dialog that blocks the bridge until a human clicks OK. A new `BreakpointDialogTracker` installs a thread-level WH_CBT hook (modelled on the Go To Definition suppressor) that reads the dialog text, dismisses it with OK, and surfaces it as `lastBreakpointDialog` on debugger state and `debug_exceptions` responses, so the caller learns the condition was rejected instead of hanging on a hidden popup.
- Added thread/frame targeting for debugger inspection tools. `debug_stack` now returns zero-based frame indexes, and `debug_locals` / `debug_watch` accept `thread_id` plus `frame_index` so agents can inspect the same stopped frame that `debug_stack` shows instead of relying on Visual Studio's currently selected debugger context. `debug_watch` also exposes `timeout_ms` through the tool catalog.
- Added model-visible debugger exception capture. The VSIX now subscribes to Visual Studio `DebuggerEvents.OnExceptionThrown` and `OnExceptionNotHandled`, stores the latest thrown/not-handled exception, clears it when execution resumes, and includes it as `lastException` in debugger state and `debug_exceptions` responses with event kind, exception type, name, code, description, action, and capture timestamp. The tool catalog, README debug examples, and debugger SDK notes now document `lastException` and `debug_exceptions` so models can find the workflow from bridge help and repo docs.
- Fixed `debug_stack` and `debug_threads` reporting the wrong thread when no `thread_id` is supplied. `DebuggerService.GetStackAsync` defaulted to `ResolveThread(program, null)`, which returns the first thread in the program (usually the main/UI thread) rather than the thread that actually hit the breakpoint — so `debug_stack` with no arguments returned an unrelated stack (e.g. the GUI render loop) while the breakpoint sat on a background/TBB worker thread, forcing callers to probe every worker by hand. `GetStackAsync` now defaults to `DebuggerFrameContextResolver.TryGetCurrentThread(debugger)`, which resolves the thread whose call stack contains `debugger.CurrentStackFrame`'s function (`StackFrame.FunctionName`, preferring the thread where it is the top frame), and only falls back to `debugger.CurrentThread` if that fails. This is necessary because `debugger.CurrentThread` was observed pointing at the main/UI thread even while the break sat on a worker thread (native multi-threaded debugging), whereas `CurrentStackFrame` (which `debug_locals`/`debug_watch` already use) correctly points at the stopped frame. EnvDTE exposes no direct `StackFrame`→`Thread` link — `StackFrames.Parent` returns the `Debugger`, not the `Thread`, and reflection over the COM RCW does not surface frame members — so the function-name match is used. `debug_threads` (`GetThreadsAsync`/`GetThreadSummaries`) now tags each thread with an `isCurrent` flag and adds a top-level `currentThreadId`, so callers can jump straight to the stopped thread. `TryGetCurrentThread` was promoted from `private` to `internal` in `DebuggerFrameContextResolver` and reused rather than duplicated. `GetStateAsync` (which backs the break-state returned by `debug_start`/`debug_continue`/`debug_break`/`debug_step_*` via `WaitForBreakOrDesignModeAsync`) now computes the same `currentThreadId` and passes it to `GetThreadSummaries`, so those responses also carry the per-thread `isCurrent` flag and a top-level `currentThreadId` instead of reporting `isCurrent:false` for every thread.
- Added `lastBreakReason` and `brokeOnException` to `debug_exceptions`. The EnvDTE engine does not expose exception-group settings (the existing `featureNotSupported` path), but it does expose `Debugger.LastBreakReason`, so `debug_exceptions` now reports *why* the debugger is stopped — `dbgEventReasonBreakpoint`, `dbgEventReasonExceptionThrown`/`dbgEventReasonExceptionNotHandled`, `dbgEventReasonUserBreak`, `dbgEventReasonStep`, etc. — plus a convenience `brokeOnException` boolean, letting a caller immediately distinguish a breakpoint hit from a thrown/unhandled exception or a manual pause instead of guessing. The read happens inside the existing UI-thread helper, renamed `CollectExceptionGroupsOnUiThread` → `CollectExceptionInfoOnUiThread`, to keep `GetExceptionsAsync`'s main-thread region small.
- Hardened `close_document all:true` and `close_others` against Visual Studio deferred-document load failures. Document close now tries the normal DTE close first, falls back to closing the owning editor window, and reports any remaining stuck tabs in `skipped`/`skippedCount` with the close exception details instead of aborting the whole close command. `reload_document` now resolves bridge file handles before reloading so handles returned by `read_file`, `find_text`, and diagnostics do not get misread as bogus drive paths such as `F:\68`.
- Added bridge-native Git range and rebase tools: `git_compare_refs`, `git_log_range`, `git_diff_range`, `git_rebase`, `git_rebase_continue`, `git_rebase_abort`, and `git_rebase_skip`. The recommendation engine now recognizes upstream/cherry-pick/rebase/branch-compare wording and points models to the bridge Git workflow before shell fallbacks; `GitRunner` also sets noninteractive editor environment variables so rebase and merge continuation do not hang waiting for an editor.
- Added proactive Visual Studio tab-pressure guidance for models. Bound-session guidance, MCP initialize instructions, recommended tools, `list_tabs`, and related close-tool hints now tell agents to inspect tab count and close inactive tabs with `close_file`, `close_document`, or `close_others` when more than 7 editor tabs are open, reducing VS overload/hang risk during long bridge sessions.
- Fixed `warnings`, `errors`, and `messages` `group_by` mode returning all groups on every chunk instead of paginating them. `DiagnosticCollection.WriteTo` now applies chunk-level pagination to the `groups` array itself and overwrites all pagination fields (`count`, `totalCount`, `filteredCount`, `chunkIndex`, `chunkSize`, `chunkCount`, `chunkStart`, `chunkEnd`, `hasMoreChunks`, `truncated`) with group-level values. Previously the row-level pagination fields were written first and only `count` was overridden, so `chunkCount` reflected individual-row count divided by chunk size (e.g. 5 chunks for 216 rows / 50) while all 70 groups were returned in every chunk — making pagination non-functional and responses oversized.

## 3.0.2

- Added `git_untrack` tool: removes files from the git index without deleting them from disk (`git rm --cached`). Accepts a `paths` array or `path` string shorthand. Use this to stop tracking files that should be `.gitignored`. Wired into task-profile detection (`LooksLikeUntrackTask`) with keywords such as `untrack`, `git rm`, `rm --cached`, `stop tracking`, `gitignore`, and `exclude from git`; appears in `recommend_tools` results and `DefaultRecommendedGitToolNames`. Also mentioned in the bound-session hint so models know to prefer it over shell `git rm --cached`.
- Fixed bridge silently re-binding to a different VS instance when the previously bound instance closes. `BridgeConnection.RetryAfterFailureAsync` now calls `VsDiscovery.IsProcessGone` after evicting the cached instance on a pipe failure. If the VS process has exited, a `SessionLostError` (-32005) is thrown with the label and PID of the closed instance and instructions to call `bridge_health` then `bind_solution` or `bind_instance` to reconnect — instead of silently auto-discovering and binding to whatever VS instance happens to be open next. Added `SessionLostError = -32005` to `BridgeConnectionDefaults` and `IsProcessGone(int processId)` helper to `VsDiscovery`.
- Fixed `set_version` tool silently claiming success when a version regex did not match a target file. `UpdateFile` in `SetVersionTool.cs` was unconditionally adding every existing file to the `updated_files` result, even when the regex found nothing to replace and no write occurred. The method now takes a `hasMatch` predicate; files whose pattern is not found are collected into a `skipped_files` list and surfaced as a `warning` in the tool output rather than being reported as updated.
- Added `build_installer` developer tool: compiles the Inno Setup installer script (`installer/inno/vs-ide-bridge.iss`) by invoking ISCC.exe, producing `installer/output/vs-ide-bridge-setup-{version}.exe`. Resolves ISCC.exe from PATH then standard Program Files install locations (Inno Setup 6 before 5, x86 before x64). Accepts optional `configuration` (default Release), `iss_file` path override, and `timeout_seconds` (default 120). Implemented in the new `InnoSetupRunner` helper, registered in the `developer_tools` category.
- Fixed `rebuild_solution` to automatically compile the Inno Setup installer after a successful `wait_for_completion=true` rebuild when a `.iss` file is detected at `installer/inno/vs-ide-bridge.iss` relative to the solution root. The installer result is included as `installerResult` in the response. For solutions without a `.iss` file, the step is silently skipped — behaviour is unchanged. `rebuild_solution` is no longer registered via `CreateBuildTool` factory; it uses a custom lambda that chains `ExecuteBuildToolAsync` + `InnoSetupRunner.RunAsync`.
- Fixed `peek_definition` and `goto_definition` showing a modal "Cannot navigate to the symbol under the caret" dialog when the caret is positioned on a non-navigable token (e.g. a C# keyword like `internal`). `PostGoToDefinitionCommandAsync` in `GoTo.cs` now installs a thread-level `WH_CBT` Windows hook before posting the Go To Definition command; the hook intercepts any `#32770`-class window with a "Go to Definition" title as it activates and auto-dismisses it with `PostMessage(WM_COMMAND/IDOK)` before it becomes visible. The hook is removed in a `finally` block after the 500 ms navigation wait, ensuring the UI thread installs and uninstalls it symmetrically.

## 3.0.1

- Added `cmake_configure` tool: runs cmake to configure a project from a source directory. Accepts `preset` (uses `--preset`) or `build_dir` (uses `-S`/`-B`), plus optional `source_dir` (defaults to the bound solution's repository root), `extra_args` string array, and `timeout_seconds` (default 180). Resolves `cmake.exe` from PATH, VS-bundled locations (VS 2026, 2022, and 2019 across Enterprise/Professional/Community/BuildTools), and `C:\Program Files\CMake\bin`. Implemented via the new `CmakeRunner` helper (modelled after `GhRunner`) and registered in the `project` tool category.
- Added `cmake_cache` tool: reads the CMake cache for a given `build_dir` and returns all cache variables. Accepts an optional `filter` substring to narrow results. Useful for inspecting configured paths, flags, and options without opening `CMakeCache.txt` directly.
- Fixed BP1012 best-practice warning in `DiagnosticsTools.cs` (file exceeded 1000-line threshold) by extracting `CreateCompileFileTool`, `EnrichCompileFileResponse`, `IsHeaderCompileTarget`, `CreateRunCodeAnalysisTool`, and `TryCaptureAnalysisDiagnosticsAsync` into a new `DiagnosticsCompileTools.cs` partial class. Both files now stay comfortably under the threshold.
- Fixed `apply_diff` `edits` array silently counting duplicate/already-applied edits as failures. `IsPatchContentMismatch` in `SafetyHelpers.cs` now catches `"old_content was not found"` errors (produced by search-block / `file`+`old_content`+`new_content` patches) in addition to the existing `"mismatch"` check (unified diff hunks), enabling the `alreadySatisfied` detection path to fire for the common targeted-edit format.
- Fixed `apply_diff` `edits` array not propagating `alreadySatisfied` in per-entry results. `ApplyMultipleEditsAsync` in `DocumentTools.cs` now reads `Data.items[0].alreadySatisfied` from the underlying patch response and includes `"alreadySatisfied": true` in the per-edit entry, so callers can distinguish freshly-applied edits from those already in the desired state.
- Added `path` string shorthand parameter to `git_add`, `git_restore`, `git_reset`, `git_diff_unstaged`, and `git_diff_staged`. Previously all five tools only accepted a `paths` array; callers can now pass `path: "src/Foo.cs"` as a convenience alias for `paths: ["src/Foo.cs"]`. `git_add` and `git_restore` also relaxed their `paths` parameter from required to optional so either form is accepted independently. New `GetEffectivePathList` and `GetEffectivePathListOrNull` helpers in `GitTools.cs` implement the coalescing logic (`paths` takes precedence; `path` is the fallback; absent → `["."]` or `null`).
- Fixed BP1025 (`warnings`/`errors` best-practice analyzer) emitting false positives for C/C++ local variable declarations and identifiers inside line comments. `IsInsideCppLineComment` now skips any regex match whose position is preceded by `//` on the same line; `IsInsideFunctionParams` walks backwards from the match and skips it unless an unmatched `(` is found before a statement boundary (`; { }`), eliminating false positives like `std::string code, comment;` (local declaration) and `vector a,b` inside comment text.
- Fixed `warnings`, `errors`, and `messages` `path` filter silently matching nothing on Windows when the caller used forward slashes (e.g. `path: "src/libslic3r"`). `DiagnosticRow.MatchesPath` now delegates to `ContainsPath`, which normalises both sides to `/` before comparing, so forward- and back-slash path filters both work against Windows-style stored paths.
- Fixed `warnings`, `errors`, and `messages` passive-cache responses embedding the full unfiltered diagnostic snapshot (all severity buckets with every row) in `Cache.snapshot`, causing responses to exceed 2 MB when thousands of warnings were present. `DocumentDiagnosticsSnapshot.ToJson` now calls `DiagnosticBucket.ToCountSummaryJson` instead of `ToDataJson`, emitting only the pre-existing count/metadata fields (no rows). The full rows are already present in the main `Data` object.

## 3.0.0

- Added `run_tests` tool: runs `dotnet test` from the bound solution directory and returns structured pass/fail results. Supports project selection (by name or path, defaulting to the active solution), configuration, framework, runtime, VSTest `--filter` expressions, loggers (`trx`, etc.), results directories, data collectors, verbosity, `--blame`, and a configurable timeout (default 120 s). The response includes a `testSummary` object with `outcome`, `passed`, `failed`, `skipped`, and `total` counts parsed from the `dotnet test` summary line, plus truncated `stdout`/`stderr` controlled by `head_lines`, `tail_lines`, and `max_lines`. `no_restore` defaults to `true`. Aliased as `test`, `dotnet_test`, `unit_tests`, `run_unit_tests`, and `vstest`.
- Fixed `apply_diff` for non-project files (`<MiscFiles>` like `CHANGELOG.md`, `.ps1`, `.txt`): `read_file` now registers and returns an `f:N` handle in `Data.handle` so models can pass it directly to `apply_diff` without a separate `find_files` call. `apply_diff` also now accepts plain paths (absolute or solution-relative) in addition to handles, so local models without the host `Edit` tool can edit any file with just `read_file` + `apply_diff`. Removed the handle-only restriction from both the single-edit and multi-edit (`edits` array) code paths. Updated tool descriptions and MCP initialize instructions accordingly.

- Fixed `github_pr_list`, `github_pr_view`, and `github_pr_diff` hanging for exactly 30 seconds when spawned from a service or background host. `GhRunner.CreateStartInfo` now sets `RedirectStandardInput = true` and closes stdin immediately after process launch so `gh` receives EOF and knows it is non-interactive; previously `gh` inherited the parent's stdin and blocked indefinitely waiting for interactive input (browser auth prompt, update check, etc.). Added `GH_PROMPT_DISABLED=1` and `BROWSER=""` environment variables as belt-and-suspenders to suppress any remaining interactive behaviour.
- Fixed `errors`, `warnings`, and `messages` reporting empty `code`, `path`, `project`, `file`, and `text` fields in the `filter` echo after service-side content filtering. The VSIX only receives severity/quick/max; content filters are applied service-side by `DiagnosticCollection`. The new `PatchFilterEcho` method in `DiagnosticsTools` overwrites the stale filter fields in the VSIX response with the actual `DiagnosticQueryOptions` that were applied, so the echo matches what was filtered.
- Added `path` parameter to `git_log` to limit history to commits that touched a specific file or directory, using LibGit2Sharp's `repo.Commits.QueryBy(path)` file-history walk (equivalent to `git log -- <path>`). Added `paths` array parameter to `git_diff_unstaged` (case-insensitive substring filter on changed paths via LibGit2Sharp) and `git_diff_staged` (appends `-- <paths>` to the git diff arguments).
- Fixed `git_push` and `git_pull` producing double-space arguments when optional remote or branch parameters are omitted. Replaced `$"push {a} {b} {c}".TrimEnd().Replace("  ", " ")` (single-pass, only collapses one pair of spaces) with `string.Join(" ", parts.Where(s => !string.IsNullOrEmpty(s)))`. Fixed `git_stash_list` being registered under `GitDiffAndMetaTools` instead of `GitStashTools`. Removed the unused `BuildPathList` dead-code helper.
- Merged PR #11 from contributor Henry Li (henrli): VS 2026 compatibility fixes and new `list_launch_profiles` / `set_launch_profile` commands (see following entries).
- Added VS 2026 compatibility: updated VSSDK Build Tools to 18.5 for VS 2026 VSIX packaging, marked `Newtonsoft.Json` and `LanguageServices` as compile-only to avoid binding conflicts with VS-provided assemblies, excluded Roslyn, Threading, Composition, and other VS-shipped DLLs from VSIX output, fixed `VisualStudioMajorVersion` (`"18"` → `"2026"` to match the folder name change), and added `global.json` for SDK build tools resolution.
- Added `list_launch_profiles` tool: reads the solution's `.slnLaunch` file and returns all named multi-project launch profiles with their project paths and actions.
- Added `set_launch_profile` tool: activates a named launch profile by exact or partial name match, setting the startup projects via DTE.
- Restored `call_hierarchy` integration: re-added the `CallHierarchy` assembly reference and `CallHierarchyWindow.cs` after confirming the DLL is present on both VS 2026 preview and canary installs.
- Fixed VSCT command ID collision: `list_launch_profiles` and `set_launch_profile` command IDs moved from `0x0265`/`0x0266` to `0x0267`/`0x0268` to avoid conflict with upstream `PeekDefinition` and `ReadOutputWindow` commands.
- Added `edits` array to `apply_diff`: callers can now pass multiple independent edits in one call instead of calling `apply_diff` repeatedly. Each element in the `edits` array is an independent `{ file, old_content, new_content }` object dispatched as its own command instance. Edits are applied in order; per-edit failures are reported in the response without aborting the remaining edits. The combined response includes `{ count, applied, failed, edits }` with per-entry success/error details.
- Improved `list_launch_profiles` and `set_launch_profile` tool descriptions: `list_launch_profiles` now explains the `.slnLaunch` JSON structure (Name, Projects array with Path/Action/DebugTarget fields) and directs callers to use it before `set_launch_profile`; `set_launch_profile` now documents exact-then-partial name matching, the ambiguity error when a partial name matches more than one profile, and which `Action` values (`Start`/`StartWithoutDebugging`) add a project as a startup project vs. `None` (excluded).
- Fixed `errors`, `warnings`, and `messages` ignoring `sort_by`, `sort_direction`, `chunk_index`, and `chunk_size` arguments. The MCP service layer (`DiagnosticsTools.HandleAsync`) now forwards all four as `--sort-by`, `--sort-direction`, `--chunk-size`, and `--chunk-index` in the bridge command string, completing the end-to-end path that was already wired on the VSIX side in 2.2.13.
- Improved MCP `initialize` instructions to advertise the full search tool hierarchy: `search_symbols` (not `find_text`) is now explicitly called out as the first choice when a symbol name is known; `file_outline` before `read_file` on unfamiliar files; `glob` (bridge) instead of the host Glob tool; `read_file_batch` for multiple slices; and `find_text_batch` for multiple patterns. A handle-prefix table (`h:`, `f:`, `e:/w:/m:`) is now included directly in the instructions so models know which tool produces which handle without consulting the category listing.
- Expanded the lazy-mode tool enumeration in `BuildInstructions` to include `read_file_batch`, `find_text_batch`, `search_symbols`, and `file_outline`, giving models a broader mental model of available tools from session start.
- Added "Seeing What the Model Is Doing" section to the README explaining how to open the VS Output window and select the **IDE Bridge** pane to watch tool calls in real time.
- Improved tool descriptions to advertise handle-pass-through across the full consuming surface. `write_file`, `file_outline`, `symbol_info`, `peek_definition`, and `find_references` descriptions now explicitly tell models to pass an `h:N` or `f:N` handle from a prior search result as the `file` argument instead of copying the full path. `find_text`, `find_text_batch`, `find_files`, and `glob` descriptions are updated to list all consuming tools (including `file_outline` and `write_file`) so models know the complete downstream chain. `find_text_batch` and `glob` were previously silent on handles entirely.
- Fixed `read_file_batch` dispatching all ranges through a single `document-slices` command, which caused VS to clobber the active editor on every iteration. Each range is now dispatched as a separate `document-slice` command instance with `reveal-in-editor: false`, so the active document is never changed during a batch read.
- Changed `read_file_batch` default sort order from `path` to `index` so slices are returned in the order they were requested. Previously, results were sorted alphabetically by file path, which could cause a model to associate the wrong slice with the wrong range when files from different directories were mixed in a single call.
- Added `HandleService` — a session-scoped handle registry that stamps short IDs (`e:N`, `w:N`, `m:N`, `f:N`, `h:N`) on every result row returned by `errors`, `warnings`, `diagnostics_snapshot`, `find_text`, `find_files`, and `search_symbols`. Pass a handle ID (e.g. `"h:3"`, `"e:1"`) as the `file` argument to any consuming tool instead of copying the full absolute path — resolution is transparent via `PathResolver` and `DocumentService` with no changes required in individual consuming commands. All handle logic lives in one class (`HandleService`) per the single-container design rule.
- Added `read_file` handle auto-anchoring: when the `file` argument is a handle that carries an embedded line number, `read_file` opens at that line without requiring an explicit `start_line` argument.
- Added `HandleService.RewritePatch` so `apply_diff` can accept handle IDs in `*** Update File: h:N` patch headers — handles are resolved to absolute paths before `PatchService` processes the text.
- Fixed BP1014 best-practice analyzer incorrectly flagging single-letter variables `m` and `n` as unclear names. Both are universally conventional in loop and sequence contexts and are added to the exemption list alongside `i`, `j`, `k`, `x`, `y`, `z`, `e`, `s`, and `_` for both C# and Python.
- Improved `apply_diff` tool description to lead with the `file` + `old_content` + `new_content` targeted form as the preferred single-file edit, relegating the `*** Begin Patch` patch format to multi-file and structural changes. Added explicit guidance that open files reload automatically before matching (no stale-buffer problem) and that Claude's built-in Edit/Write tools must never be used for files open in VS.
- Strengthened bridge help/tool schema guidance for lazy `call_tool` object arguments and handle reuse: initialize instructions, `tool_help` handle guide, file argument descriptions, and read/apply docs now show `file: "h:2"` with `start_line`/`end_line` and tell models to use handles as canonical session references.
- Hardened `apply_diff` validation: mixed targeted+diff requests now fail with a one-shape message, and simple single-file no-context replacement patches are rejected so models use `file` + `old_content` + `new_content` instead of hand-built patch syntax.
- Fixed `apply_diff` error message when content matching fails: previously the error always said "the @@ context block was not found" and told the model to "Build a new @@ block", even when the caller used the `file` + `old_content` + `new_content` form. The fix detects the simple-replace form (no context lines in the patch block) and emits a distinct message — "old_content was not found" — with instructions to copy the exact text from `read_file` into `old_content`, not to construct a patch block.
- Fixed BP1043 best-practice warnings in `IdeListLaunchProfilesCommand` and `IdeSetLaunchProfileCommand` by extracting the background-thread body into `BuildProfileList` and `ApplyProfileAsync` helper methods, leaving `ExecuteAsync` with only the minimal main-thread read + background hand-off.
- Fixed BP1013 best-practice warning in `ProjectManagementTools` (method exceeded 100-line threshold) by splitting it into three focused sub-methods: `ProjectSolutionTools`, `LaunchProfileTools`, and `ProjectFileTools`, joined by a one-liner `Concat` chain in `ProjectManagementTools`.
- Fixed BP1031 and BP1032 best-practice warnings in `install-vsix.ps1`: replaced all `Write-Host` calls with `Write-Output`, and added `Set-StrictMode -Version Latest` plus `$ErrorActionPreference = 'Stop'` at the top of the script.
- Improved `read_file` tool description to lead with "ONE file per call, always a windowed range, never the full file", add explicit "to continue past what you've seen: call again with start_line = (last line shown + 1)", and direct multi-file or multi-slice use to `read_file_batch`. Updated the `start_line` and `end_line` parameter descriptions to reinforce the no-full-file constraint and the continuation pattern. Updated `read_file_batch` short summary and description to make explicit that each range item carries its own file path, so different files can be read in a single call — "do not loop read_file."
- Improved `file_outline`, `search_symbols`, and `call_hierarchy` tool descriptions to open with explicit behavioral guidance — when to call each tool and what it replaces (`file_outline` now directs calling it before `read_file` on any unfamiliar file; `call_hierarchy` now directs using it instead of looping `find_text` → `read_file` to trace call chains; `search_symbols` now directs using it first when the target is a named symbol, not `find_text`).
- Fixed `apply_diff` and `read_file` tool descriptions to explicitly show the `call_tool` invocation pattern. `McpServerMode.BuildInstructions` now says "ALL bridge tools must be called through call_tool" and adds an `apply_diff` example alongside the existing `read_file`/`errors`/`git_status` examples. The `apply_diff` description now leads with the `call_tool` invocation and the `diff` parameter description opens with "ONLY for multi-file changes — for a single file use file+old_content+new_content instead."
- Removed `list_tools` from the lazy MCP top-level surface (`McpToolSurface.LazyToolNames`). It is still callable via `call_tool { "name": "list_tools" }` but no longer appears in `tools/list`, preventing models from reflexively dumping the full 150-tool catalog on connect. Updated `BuildInstructions`, `BuildToolDiscoveryGuidance`, and `BuildBoundRecommendedTools` to reflect the change; the `list_tools` tool description already noted "Names returned here are not necessarily directly exposed MCP tools in lazy mode."
- Changed the default local MCP HTTP ports away from the common `8080` default: shared HTTP now uses `http://localhost:43117/`, and Streamable HTTP now uses `http://localhost:43118/mcp`.
- Fixed the installer HTTP option to persist both the legacy shared HTTP flag and the Streamable HTTP flag, so starting the Windows service after install brings up the correct Streamable HTTP listener as well as the legacy endpoint.
- Added 2024-11-05 HTTP+SSE compatibility on the Streamable HTTP listener: clients can open `/sse`, receive an endpoint event, and POST JSON-RPC messages to `/messages?sessionId=...` while newer clients continue to use `/mcp`.
- Added `streamable_http_enable`, `streamable_http_disable`, and `streamable_http_status` catalog tools, and clarified that the existing `http_*` tools manage the legacy direct-POST HTTP endpoint.
- Centralized HTTP port defaults in `HttpServerDefaults` so the VSIX, Windows service, foreground HTTP hosts, tool descriptions, and setup docs do not drift apart.
- Fixed `apply_diff` verification for project and MSBuild files (`.csproj`, `.vcxproj`, `.props`, `.targets`, and `.sln`) by treating disk-backed patch files as disk source-of-truth instead of reading stale open Visual Studio project-file buffers.
- Added the lazy MCP tool surface so default `tools/list` stays compact while `call_tool`, `recommend_tools`, `tool_help`, and `list_tools` still expose the full bridge surface on demand.
- Improved tool recommendations for local coding models by recognizing common search/read/edit/run wording plus Claude, Codex, Qwen, Gemini, Grok, and DeepSeek provider cues.
- Documented the Visual Studio `Tools → VS IDE Bridge → Toggle Streamable HTTP MCP Server` path for enabling the service-owned Streamable HTTP MCP endpoint, and renamed the root menu command from `IdeBridgeMenu` to `VsIdeBridgeMenu`.
- Fixed explicit build/rebuild waits to use the Visual Studio build completion path and longer build timeout instead of falling back into courtesy polling loops.
- Fixed post-filtered search results so path and project filters accept either forward slashes or backslashes, keeping `find_text` and `find_text_batch` counts aligned with returned rows.
- Added an explicit VSCT `MenuText` for the Tools menu so Visual Studio displays `VS IDE Bridge` instead of a command/symbol name.
- Moved bridge file logs to `C:\Program Files\VsIdeBridge\logs` (the install directory) via a registry key written by the installer, so both the service and the VSIX extension write to the same location regardless of process identity.
- Fixed git tools routing to a parent mega-repo when the solution directory has no `.git` of its own. `ResolveRepoRootDirectory` now verifies that at least one tracked file exists under the solution directory before accepting an ancestor repo root, and `OpenRepository` adds a post-open mismatch guard that throws a clear error naming both the expected and found paths if `Repository.Discover` walked up to the wrong repository.
- Updated the MCP `initialize` instructions to tell models that bridge tools are for autonomous use: do not ask the user which tools to call, do not present tool names or lists to the user, and do not wait for approval before calling a tool. Added three concrete `call_tool` JSON examples so weaker models have an unambiguous pattern to follow.
- Updated the `vs_state` response summary to include next-step guidance naming the most common follow-on tools (`errors`, `read_file`, `find_text`, `list_projects`, `list_tool_categories`), so models do not stall after a successful state check.
- Fixed CA2249: replaced `IndexOf(...) >= 0` with `Contains(...)` in `BestPracticeAnalyzerHelpers.cs`.
- Added `github_pr_list`, `github_pr_view`, and `github_pr_diff` tools backed by the GitHub CLI (`gh`). `GhRunner` resolves the executable from PATH and common install locations and throws a clear error with install instructions if `gh` is not found.
- Fixed `git_add`, `git_reset`, `git_restore`, `git_commit`, and `git_commit_amend` using LibGit2Sharp in-process instead of a subprocess. The subprocess hung for 30 seconds under the Windows service account due to index lock contention with VS's own git integration; LibGit2Sharp avoids all subprocess overhead and works reliably in the service context.
- Changed `errors`, `warnings`, and `messages` command summaries to always show a full severity breakdown — `"0 error(s) · 1 warning(s) · 2 message(s) — fix all before building."` — regardless of which severity was filtered. Previously the summary only showed the filtered count, so models could see `"0 errors"` and declare clean while warnings and messages remained.
- Expanded `postCheck` (returned by `apply_diff` and `write_file`) to include `warningCount`, `messageCount`, and a `summary` string with the same full breakdown, so models cannot declare success after an edit without seeing the complete diagnostic state.
- Fixed `ErrorListService` generating a `[WARNING]` log entry with full stack trace every time the Error List window is not open. The window being absent is a normal startup condition; `ReadDteRowsAsync` now returns the empty row list directly instead of throwing, and the surrounding try/catch in `GetErrorListAsync` is removed.
- Fixed `DocumentService.TryShowActivePoint` logging a `[WARNING]` with full stack trace on transient COM `E_UNEXPECTED` failures when centering the viewport. Viewport centering is best-effort (the method is named `Try...`); demoted to `LogVerbose` which writes to the debug output only, not to the log file.
- Fixed `BuildService.RunCodeAnalysisAsync` hanging until the deadline when `Build.RunCodeAnalysisonSolution` fails to start. The `ExecuteCommand` failure was caught and logged but `WaitForBuildEventAsync` continued waiting for a build completion event that would never arrive. Added a `CancellationTokenSource` (`launchCts`) that is cancelled on `COMException`; a linked token combines it with the request cancellation token so the wait unblocks immediately and a clear `CommandErrorException("analysis_launch_failed", ...)` is thrown instead of a timeout.
- Added log retention to `BridgeLogPaths`: `PruneExtensionLogs` deletes daily `vs-ide-bridge-*.log` files older than 7 days (called once per process on first write from `BridgeActivityLog`); `RotateMcpServerLog` renames `mcp-server.log` to `mcp-server.log.old` when it exceeds 5 MB (called at service startup from `McpServerLog.ResolveLogPath`), capping total log footprint to ~10 MB.
- Added `BridgeLogPaths.CleanupLegacyLogs` to delete stale log files left in `%COMMONAPPDATA%\VsIdeBridge\logs` from versions before 2.3.0 moved logs to the install directory. Called once at startup from both `McpServerLog.ResolveLogPath` and `BridgeActivityLog.WriteToFile` (guarded by the existing `_prunedOnce` flag) so the old location is cleaned up automatically on the first run after upgrading.
- Added `bridge_log_summary` tool (`developer_tools` category) backed by `BridgeLogSummaryTool`: reads the tail of the MCP server log and the active VS extension log, classifies lines as errors, warnings, lifecycle events, or request events, and returns structured JSON with counts and per-event detail. Supports `log` (all/mcp/extension), `tail_lines`, `max_events`, `text` filter, and `include_raw`. Replaces the need to open log files directly in the editor during bridge diagnostics.

## 2.2.13

- Added paged diagnostics retrieval so large warning, error, and message sets can be read in chunks with counts, offsets, filters, and sorting instead of flooding the model with every row at once.
- Added Visual Studio Output window capture support, including build-output panes, so diagnostics can fall back to structured output chunks when the Error List is empty, stale, or too large.
- Updated installer Visual Studio discovery to locate `VSIXInstaller.exe` with `vswhere` across installed and prerelease VS versions, with fallback Program Files scanning for older layouts.
- Fixed `read_file` returning a wall of JSON — the redundant per-line `lines` array and `queue` scheduling metadata are now stripped from the tool result so only the human-readable slice text is surfaced.
- Fixed `read_file_batch` returning only "Captured N slice(s)." — the summary now renders each slice's file header and content so the model can read code without an extra round-trip.
- Fixed `read_file_batch` silently ignoring `context_before` and `context_after` in anchor (`line`) mode when parameters were sent with underscore names, causing slices to collapse to a single line.
- Updated `read_file_batch` tool description to explicitly document both range modes: `start_line`+`end_line` for a fixed range, and `line`+`context_before`+`context_after` for an anchor-based slice.
- Narrowed broad `Exception` catches to `COMException` in COM enumerator loops (solution project list, project items, open documents) to satisfy the BP1034 analyzer and avoid masking unrelated errors.
- Improved best-practice comment analyzer: XML documentation comments (`///`) are no longer flagged as low-value, and redundant-prefix detection (`this method`, `gets the`, etc.) now only fires when the phrase opens the comment rather than appearing anywhere in it.

## 2.2.12

- Made `http_enable` and `http_disable` reconcile the service-owned HTTP listener instead of only toggling local process state. When called from a short-lived stdio MCP child the toggle now reaches the long-running service over the control pipe, so the listener on `localhost:8080` actually starts or stops and the persisted enable flag stays in sync. Falls back to the in-process listener when the service is not installed.
- Fixed the service control pipe accepting only one client at a time. A single long-lived control client (such as the stdio MCP child sending `MCP_REQUEST`) was blocking every other process — including `http_enable`/`http_disable` — from connecting. The accept loop now hands each connection to a per-client task and immediately loops back to accept the next.
- Made `http_status` cross-process honest. It now reports the live `localhost:8080` listener probe alongside the persisted enable flag, and surfaces a `note` describing what reconciliation actually did (service confirmed listener up / down, or fell back to in-process).
- Added `HttpServerController.IsPortListening` as the cross-process truth source so callers can tell whether a listener is really accepting connections versus only consulting the in-process `IsRunning` flag (which is process-local and now documented as such).
- Hardened `HttpServerController.Disable()` to handle `IOException` and `UnauthorizedAccessException` when deleting the flag file instead of letting them bubble up and abort the disable path.
- Centralized the service control pipe name in `NamedPipeAccessDefaults.ServiceControlPipeName` so the VSIX, the service, and the control client share a single source of truth, and added `NotifyHttpEnableAsync` / `NotifyHttpDisableAsync` to `ServiceControlClient` for callers that prefer the typed wrapper.
- Refreshed the `http_enable`, `http_disable`, and `http_status` tool descriptions in the catalog to describe the service-reconciliation behavior.

## 2.2.11

- Switched the installer and installed runtime fully to the service-backed MCP layout. The release removes the stale `cli` payload, fixes uninstall metadata, and adds installer support for enabling the local HTTP MCP endpoint.
- Fixed bridge discovery and command dispatch issues that could hide tools or break batched requests. This includes better discovery defaults, `glob` visibility, typed `batch` step arguments, and safer `BridgeCommand` registration behavior.
- Improved diagnostics and build behavior for large solutions. Passive warning and message reads now avoid unnecessary UI refreshes by default, quick fallback paths are stronger, and long-running solution builds report background progress without burning tokens.
- Reduced UI-thread stalls during search and investigation. `find_text_batch` now captures the search snapshot once per batch, and watchdog telemetry now records the active command when a probe timeout is detected.
- Cleaned analyzer and message-level issues in the bridge repo instead of suppressing them, and updated MCP setup documentation for both Codex and Claude Code with the current installed service path and config shapes.
